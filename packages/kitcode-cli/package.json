{
  "name": "@onedigitas/kitcode",
  "version": "0.1.6",
  "description": "Local aggregate activity server for KitCode.",
  "license": "MIT",
  "type": "module",
  "bin": {
    "kitcode": "bin/kitcode.mjs"
  },
  "files": [
    "bin",
    "src",
    "README.md",
    "LICENSE"
  ],
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "node scripts/check-integration-spec.mjs && node --check bin/kitcode.mjs && for file in src/*.mjs; do node --check \"$file\"; done",
    "lint": "npm run build"
  },
  "dependencies": {
    "express": "^4.21.2"
  }
}
