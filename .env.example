# Optional comma-separated extra hosted web origins allowed to connect to the local KitCode server.
# Localhost development origins and https://kitcode.vercel.app are allowed by default.
KITCODE_ALLOWED_ORIGINS="https://your-kitcode-web.example"

# Optional active-time target for reward progress. Defaults to 3600 seconds.
KITCODE_REWARD_SECONDS="3600"
