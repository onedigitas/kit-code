# Optional comma-separated hosted web origins allowed to connect to the local KitCode server.
# Localhost development origins are allowed by default.
KITCODE_ALLOWED_ORIGINS="https://your-kitcode-web.example"

# Optional active-time target for reward progress. Defaults to 7200 seconds.
KITCODE_REWARD_SECONDS="7200"
