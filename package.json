{
  "name": "kit-code",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "npm run dev -w @kitcode/web",
    "build": "npm run build -ws",
    "lint": "npm run lint -ws",
    "pack:cli": "npm pack -w kitcode --dry-run"
  }
}
