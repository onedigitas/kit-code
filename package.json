{
  "name": "kit-code",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "npm run dev -w @kitcode/web",
    "build": "npm run build --workspaces",
    "lint": "npm run lint --workspaces",
    "pack:cli": "npm pack -w @onedigitas/kitcode --dry-run"
  }
}
